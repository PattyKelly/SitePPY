# requests: chamadas HTTP
requests>=2.30.0
# pandas: manipulaÃ§Ã£o e exportaÃ§Ã£o de CSV/Excel
pandas>=2.1.0
# numpy: dependÃªncia comum para manipulaÃ§Ã£o numÃ©rica (pandas depende em alguns casos)
numpy>=1.26.0
# python-dotenv: carregar .env localmente (NUNCA commitar .env)
python-dotenv>=1.0.0
# openpyxl: leitura/escrita de arquivos Excel (xlsx)
openpyxl>=3.1.0
# beautifulsoup4 + lxml: scraping HTML robusto
beautifulsoup4>=4.12.2
lxml>=4.9.3
# aiohttp: requisiÃ§Ãµes assÃ­ncronas (opcional/Ãºtil em scraping de alta performance)
aiohttp>=4.1.0
# openai: cliente oficial para usar a API OpenAI
openai>=1.0.0
# schedule: agendamento simples (caso queira rodar localmente)
schedule>=1.2.0
# validators: validaÃ§Ã£o de URLs / emails
validators>=0.20.0
# seaborn/matplotlib (opcional) para grÃ¡ficos nos relatÃ³rios
matplotlib>=3.8.0
seaborn>=0.13.2
# extras para automaÃ§Ã£o/CLI (opcionais)
click>=8.1.7
# (Se usa selenium para scraping complexo -> ativar quando necessÃ¡rio)
# selenium>=4.10.0
# DependÃªncias principais
requests>=2.30.0
pandas>=2.1.0
numpy>=1.26.0
# Carregar variÃ¡veis de ambiente local (.env)
python-dotenv>=1.0.0
# Excel & IO
openpyxl>=3.1.0
# Web scraping
beautifulsoup4>=4.12.2
lxml>=4.9.3
aiohttp>=4.1.0          # requisiÃ§Ãµes assÃ­ncronas (opcional)
# OpenAI
openai>=1.0.0
# Agendamento simples local
schedule>=1.2.0
# ValidaÃ§Ãµes
validators>=0.20.0
# VisualizaÃ§Ã£o (opcional para relatÃ³rios)
matplotlib>=3.8.0
seaborn>=0.13.2
# CLI / automaÃ§Ã£o (opcional)
click>=8.1.7
# Selenium: ativar somente se for realmente necessÃ¡rio (requer chromedriver)
# selenium>=4.10.0
requests>=2.28.0
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.2
matplotlib>=3.8.0
seaborn>=0.13.2
openai>=1.0.0
schedule>=1.2.0
click>=8.1.7
validators>=0.20.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
selenium>=4.10.0
python-dotenv>=1.0.0
xlsxwriter>=3.1.2
# Tente isto primeiro
.\.venv\Scripts\Activate.ps1
# se der erro de polÃ­tica, rode (uma vez, como administrador se possÃ­vel) â”€ opcional:
# Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
# Depois tente ativar de novo.
---
# 13) Checklist QA / LGPD / SEO (rÃ¡pido)
- [ ] title / meta description Ãºnicos por pÃ¡gina (OG tags)
- [ ] imagens com `alt` e dimensÃµes
- [ ] sitemap.xml + robots.txt
- [ ] consent banner (incluÃ­do no HTML de exemplo)
- [ ] aria-labels em menus, landmark roles
- [ ] cookies: nÃ£o carregar analytics/ads antes do consent
- [ ] headers de seguranÃ§a (HSTS, CSP) no servidor / Netlify
- [ ] revisar links afiliados (rel="sponsored noopener nofollow") â€” jÃ¡ tratado no script JS
- [ ] tests: validar HTML (W3C) e Lighthouse run
---
# 14) Onde colocar os secrets (resumo)
- **GitHub Actions**:
  - `GCP_SA_KEY` â†’ conteÃºdo JSON da service account
- **Netlify** (Environment variables):
  - `OPENAI_API_KEY` â†’ chave OpenAI
  - `GCP_SA_KEY` (se quiser que Netlify functions use Sheets diretamente)
- **Vercel**: equivalentes em Project Settings > Environment Variables
---
# 15) Comandos rÃ¡pidos para comeÃ§ar (copy/paste)
No Windows PowerShell (adaptar caminhos):
# 1. clonar
git clone <url-repo>
cd repo
# 2. node setup
npm install
# 3. python venv
python -m venv .venv
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser  # se necessÃ¡rio
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
# 4. rodar site local (Astro)
npm run dev
# 5. gerar relatorio python local
python scripts/relatorio.py --out relatorio_ataques_pitbull_g1.csv
# 6. testar funÃ§Ã£o netlify (opcional)
# instalar netlify cli: npm i -g netlify-cli
netlify dev
